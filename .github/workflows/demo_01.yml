name: hello_world
'on': push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
          chmod +x .github/workflows/script.sh
          .github/workflows/script.sh
          shell: bash
